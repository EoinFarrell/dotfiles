[defaults]
forks=10
# Keep auto-discovery (an explicit path would be machine/arch-specific —
# Linuxbrew's python on Debian, a different one on macOS), just silence
# the "future installation could change the discovered interpreter"
# warning it prints on every run.
interpreter_python=auto_silent
